apiVersion: v1
kind: Service
metadata:
  name: tenant-acme
  labels:
    app: tenant-acme
spec:
  selector:
    app: tenant-acme
  ports:
    - name: http
      port: 4000
      targetPort: 4000
      protocol: TCP
  type: ClusterIP
